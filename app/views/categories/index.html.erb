<h1>Categories</h1>


  <ul>
    <% @categories.each do |category| %>
    <li>
      <% category.name %>
      <%= link_to "#{category.name}", category_category_products_path(category.id) %>
    </li>
  </ul>
</section>
<% end %>
