<h1>Categories</h1>


  <ul>
    <% @categories.each do |category| %>
    <li>
      <% category.name %>
      <%= link_to "#{category.name}", category_products_path(category.id) %>
      <%= link_to "Add product", new_category_product_path(category) %>
    </li>
  </ul>
</section>
<% end %>
