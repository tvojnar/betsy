<section class="form">
  <%= form_tag do %>
    <%# label_tag :merchant %>
    <%= collection_select(:merchant, :id, Merchant.all, :id, :name, {:prompt => 'Search by Merchant'}) %>
  <%end%>
</section>
