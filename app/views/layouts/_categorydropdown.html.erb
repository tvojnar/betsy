<section class="form">
  <%= form_tag do %>
    <%# label_tag :merchant %>
    <%= collection_select(:category, :id, Category.all, :id, :name, {:prompt => 'Search by Category'}) %>
  <%end%>
</section>
